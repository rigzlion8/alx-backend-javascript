/**
 * function that that prints in STDOUT the string argument.
 * @param {String} message to display
 * @author Reagan Musa <https://github.com/rigzlion8>
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
